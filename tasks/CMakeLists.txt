# Generated from orogen/lib/orogen/templates/tasks/CMakeLists.txt

include(temperatureTaskLib)
ADD_LIBRARY(${TEMPERATURE_TASKLIB_NAME} SHARED 
    ${TEMPERATURE_TASKLIB_SOURCES})
add_dependencies(${TEMPERATURE_TASKLIB_NAME}
    regen-typekit)

TARGET_LINK_LIBRARIES(${TEMPERATURE_TASKLIB_NAME}
    ${OrocosRTT_LIBRARIES}
    ${TEMPERATURE_TASKLIB_DEPENDENT_LIBRARIES})
SET_TARGET_PROPERTIES(${TEMPERATURE_TASKLIB_NAME}
    PROPERTIES LINK_INTERFACE_LIBRARIES "${TEMPERATURE_TASKLIB_INTERFACE_LIBRARIES}")
SET_TARGET_PROPERTIES(${TEMPERATURE_TASKLIB_NAME}
    PROPERTIES INTERFACE_LINK_LIBRARIES "${TEMPERATURE_TASKLIB_INTERFACE_LIBRARIES}")

INSTALL(TARGETS ${TEMPERATURE_TASKLIB_NAME}
    RUNTIME DESTINATION bin
    LIBRARY DESTINATION lib/orocos)

INSTALL(FILES ${TEMPERATURE_TASKLIB_HEADERS}
    DESTINATION include/orocos/temperature)

